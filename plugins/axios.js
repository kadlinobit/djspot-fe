// TBD - REMOVE PROBABLY, LEAVING JUST FOR EXAMPLE
// export default function ({ $axios, redirect }) {
//     $axios.onError((error) => {
//         const code = parseInt(error.response && error.response.status)
//         if (code === 401) redirect('/login')
//     })
// }
