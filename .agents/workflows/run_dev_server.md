---
description: Start the Nuxt v4 development server
---

# Start Dev Server

This is a Nuxt v4 app. The development server runs on `localhost:3000/en`.

To spin up the dev server and automatically watch for changes, use the following command:

```bash
npm run dev
```

// turbo

```bash
npm run dev
```
